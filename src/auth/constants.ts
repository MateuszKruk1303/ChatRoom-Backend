export const JWT = 'jwt';

export const JWT_PREFIX_LENGTH = 7;

export class paths {
  static root = '/auth';
  static register = '/register';
  static login = '/login';
  static all = '/all';
  static me = '/me';
}
